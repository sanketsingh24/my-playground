// Import Actions
import {  } from './PollsActions';

// Initial State
const initialState = {};

const PollsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default PollsReducer;
