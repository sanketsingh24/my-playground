// Import Actions
import {  } from './EditProfileActions';

// Initial State
const initialState = {};

const EditProfileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default EditProfileReducer;
